<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/surface_color">

    <!-- Loading -->
    <ProgressBar
        android:id="@+id/progressGrupos"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="32dp"
        android:visibility="gone" />

    <!-- Estado de sync -->
    <TextView
        android:id="@+id/tvSyncStatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingVertical="6dp"
        android:textSize="12sp"
        android:textColor="@color/search_status_text"
        android:visibility="gone" />

    <!-- Tarjetas de grupos -->
    <!-- scrollGrupos: el fragment le aplica WindowInsets para que la
         ultima tarjeta no quede oculta detras del toolbar inferior -->
    <ScrollView
        android:id="@+id/scrollGrupos"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="20dp"
        android:clipToPadding="false">

        <LinearLayout
            android:id="@+id/containerGrupos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingBottom="20dp" />

    </ScrollView>

</LinearLayout>